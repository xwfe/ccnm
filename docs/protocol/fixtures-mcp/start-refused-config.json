{
  "$schema_ref": "#/$defs/startup_error",
  "$note": "本机或远端配置缺失/解析不了。",
  "message": {
    "exit_code": 1,
    "stderr_last_line": "CCNM_E_CONFIG: no node named runtime in this machine's config"
  }
}
