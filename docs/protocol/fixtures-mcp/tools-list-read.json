{
  "$schema_ref": "#/$defs/tools_list_result_read",
  "$note": "read 模式的工具表：7 个（P36 起多了只读的 load_skill，P39 起多了只读的 view_image，P40 起多了只读的 read_notebook；这份样例的工作区没有 skill，所以它的 description 是没有目录的那一版——有 skill 时 description 后半段是目录，见协议第 5.1 节）。没有 exec_command，没有 apply_patch，也没有 read_output——read 模式产不出 output_ref。工具名、description、inputSchema 里的参数名和 required 都是精确的，由 external_mcp 的 published_tool_tables_match_the_running_server 对着真实 server 逐字节校验；只有每个参数内部的类型、上下界和说明是简写，server 发的是 schemars 从 Rust 类型生成的完整 schema。",
  "message": {
    "jsonrpc": "2.0",
    "id": 2,
    "result": {
      "tools": [
        {
          "name": "workspace_info",
          "description": "Name, git status and platform of the remote workspace. Call once to orient; all other tool paths are relative to this workspace.",
          "inputSchema": {
            "type": "object",
            "properties": {}
          },
          "annotations": {
            "readOnlyHint": true,
            "openWorldHint": false
          }
        },
        {
          "name": "read_file",
          "description": "Read a text file from the remote workspace, as numbered lines. Paths are relative to the workspace root. Long files come back truncated with the line to resume from; there is no way to read a file whole in one call.",
          "inputSchema": {
            "type": "object",
            "additionalProperties": true,
            "properties": {
              "path": {
                "type": "string"
              },
              "start_line": {
                "type": "integer"
              },
              "end_line": {
                "type": "integer"
              },
              "max_lines": {
                "type": "integer"
              },
              "max_bytes": {
                "type": "integer"
              }
            },
            "required": [
              "path"
            ]
          },
          "annotations": {
            "readOnlyHint": true,
            "openWorldHint": false
          }
        },
        {
          "name": "list_files",
          "description": "List a directory of the remote workspace, or search it with a glob. Without a glob you get the immediate children of one directory; with one you get every match under it, at any depth. In a git workspace, files that .gitignore rules out are never listed.",
          "inputSchema": {
            "type": "object",
            "additionalProperties": true,
            "properties": {
              "path": {
                "type": "string"
              },
              "glob": {
                "type": "string"
              },
              "max_entries": {
                "type": "integer"
              },
              "include_hidden": {
                "type": "boolean"
              }
            }
          },
          "annotations": {
            "readOnlyHint": true,
            "openWorldHint": false
          }
        },
        {
          "name": "search_text",
          "description": "Search the remote workspace for a string, or a regex if you ask for one. The search runs where the files are and only the results come back: matching lines by default, or with output_mode just the file names or a count per file. Files that .gitignore rules out are never searched, dotfiles only with include_hidden, and .git never.",
          "inputSchema": {
            "type": "object",
            "additionalProperties": true,
            "properties": {
              "query": {
                "type": "string"
              },
              "path": {
                "type": "string"
              },
              "glob": {
                "type": "string"
              },
              "regex": {
                "type": "boolean"
              },
              "case_sensitive": {
                "type": "boolean"
              },
              "context_lines": {
                "type": "integer"
              },
              "max_results": {
                "type": "integer"
              },
              "output_mode": {
                "type": "string",
                "enum": [
                  "content",
                  "files_with_matches",
                  "count"
                ]
              },
              "multiline": {
                "type": "boolean"
              },
              "type": {
                "type": "string"
              },
              "include_hidden": {
                "type": "boolean"
              }
            },
            "required": [
              "query"
            ]
          },
          "annotations": {
            "readOnlyHint": true,
            "openWorldHint": false
          }
        },
        {
          "name": "load_skill",
          "description": "Load one of this project's skills: task instructions the project keeps in .claude/skills, .claude/commands or .agents/skills. Before starting a task that a skill below describes, call this with the skill's name and follow what comes back. A skill's scripts and reference files are ordinary workspace files under its directory: read them with read_file and run them with exec_command. Without a name this returns the full list.\n\nThis workspace has no skills right now.",
          "inputSchema": {
            "type": "object",
            "additionalProperties": true,
            "properties": {
              "name": {
                "type": "string"
              },
              "arguments": {
                "type": "string"
              }
            }
          },
          "annotations": {
            "readOnlyHint": true,
            "openWorldHint": false
          }
        },
        {
          "name": "view_image",
          "description": "Look at a PNG, JPEG, GIF or WebP image in the remote workspace. The image comes back as an image the model can see, up to 3932160 bytes; nothing is converted on that machine. In Codex Code Mode, pass the image block from the result to image() to see it.",
          "inputSchema": {
            "type": "object",
            "additionalProperties": true,
            "properties": {
              "path": {
                "type": "string"
              }
            },
            "required": [
              "path"
            ]
          },
          "annotations": {
            "readOnlyHint": true,
            "openWorldHint": false
          }
        },
        {
          "name": "read_notebook",
          "description": "Read a Jupyter notebook from the remote workspace as cells: each cell's id, type and source, then a code cell's outputs, with PNG and JPEG outputs as images. Long notebooks come back in parts with the start_cell to continue from. Change cells with apply_patch op edit_notebook, using the ids shown here.",
          "inputSchema": {
            "type": "object",
            "additionalProperties": true,
            "properties": {
              "path": {
                "type": "string"
              },
              "start_cell": {
                "type": "integer"
              }
            },
            "required": [
              "path"
            ]
          },
          "annotations": {
            "readOnlyHint": true,
            "openWorldHint": false
          }
        }
      ]
    }
  }
}
