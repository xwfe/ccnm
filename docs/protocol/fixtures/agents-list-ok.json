{
  "$schema_ref": "#/$defs/agents_list_response_ok",
  "$note": "只反映当前配置：不探测网络、不检查登录、不启动任何进程。条目里没有 provider——那是 Agent Node 权威解析的，而 ccnm rpc 跑在 Runtime Node 上，配置里根本没有这份定义。",
  "message": {
    "jsonrpc": "2.0",
    "id": 2,
    "result": {
      "agents": [
        {
          "node": "work",
          "instance": "claude-main",
          "workspaces": [
            "ccnm"
          ]
        },
        {
          "node": "work",
          "instance": "codex-review",
          "workspaces": []
        }
      ]
    }
  }
}
