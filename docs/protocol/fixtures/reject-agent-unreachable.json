{
  "$schema_ref": "#/$defs/response_error",
  "$note": "到 Agent Node 的 SSH 不通。effect 是 none：连都没连上，什么都没发生。",
  "message": {
    "jsonrpc": "2.0",
    "id": 30,
    "error": {
      "code": -32004,
      "message": "cannot reach the agent node over ssh",
      "data": {
        "effect": "none",
        "ccnm_code": "CCNM_E_AGENT_UNREACHABLE"
      }
    }
  }
}
