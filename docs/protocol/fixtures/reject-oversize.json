{
  "$schema_ref": "#/$defs/response_error",
  "$note": "超过 1 MiB 的请求行被整行丢弃，连接不断——分隔符是换行，读到下一个换行就重新对齐了。id 是 null：那一行没读完，也就无从知道它的 id。",
  "message": {
    "jsonrpc": "2.0",
    "id": null,
    "error": {
      "code": -32600,
      "message": "request line exceeds the 1 MiB limit",
      "data": {
        "effect": "none",
        "reason": "oversize"
      }
    }
  }
}
