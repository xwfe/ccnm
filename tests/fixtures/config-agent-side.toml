# What `ccnm init --home <alias>` writes on the work machine: how to reach
# the projects, and deliberately nothing about them. The workspace list
# lives on one machine, because two lists are two answers to "where is
# this project".
[nodes.runtime]
ssh_from_agent = "no-such-host-for-tests"
