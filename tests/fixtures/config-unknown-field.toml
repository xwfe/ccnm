# "runtime_hots" is a typo for "runtime_node". Must be rejected, not
# ignored: with the default it would silently point at [nodes.runtime].
version = 1

this = "runtime"

[nodes.agent]
ssh = "agent-alias"

[nodes.runtime]

[workspaces.xshun]
agent_node = "agent"
runtime_hots = "runtime"
root = "/Users/fodelf/Projects/xshun"
