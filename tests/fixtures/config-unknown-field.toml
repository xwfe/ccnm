# "runtime_hots" is a typo for "runtime_host". Must be rejected, not
# ignored: with the default it would silently point at [hosts.home].
version = 1

[hosts.work]
ssh = "work"

[hosts.home]
ssh_from_work = "ccnm-home"

[workspaces.xshun]
work_host = "work"
runtime_hots = "home"
root = "/Users/fodelf/Projects/xshun"
