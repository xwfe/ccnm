# The example from design doc section 5: every optional field at its
# default, so this is the smallest config that names a workspace.
version = 1

[hosts.work]
ssh = "work"

[hosts.home]
ssh_from_work = "ccnm-home"

[workspaces.xshun]
work_host = "work"
root = "/Users/fodelf/Projects/xshun"
