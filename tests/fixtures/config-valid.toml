# The smallest config that names a workspace: one other node to reach, and
# every optional field at its default.
version = 1

this = "runtime"

[nodes.agent]
ssh = "agent-alias"

[nodes.runtime]

[workspaces.xshun]
agent_node = "agent"
root = "/Users/fodelf/Projects/xshun"
